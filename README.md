# appgw-external-dns

```
cat <<EOF > .env
ENV=test
AZURE_CLIENT_ID=foo
AZURE_TENANT_ID=foo
AZURE_CLIENT_SECRET=foo
SUBSCRIPTION_ID=foo
RESOURCE_GROUP_NAME=test-rg
DNS_ZONE_NAME=foo.bar
APPGW_IP_ADDRESS=127.0.0.1
APPGW_NAME=test-appgateway
STORAGE_ACCOUNT_NAME=tfstate27798
CONTAINER_NAME=tfstate
CONTAINER_KEY=terraform.tfstate
ARM_ACCESS_KEY=foo
EOF
```
